doctype html
header
	a(href="/") 首页
	| 
	if user
		span 欢迎, 
		if user.avatar
			img(src="/avatars/" + user.avatar, alt="")
		<a href="/user/#{user.userId}">#{user.name}</a>
		| 
		a(href="/add-post") 发布
		| 
		a(href="/logout") 退出
	else
		a(href="/login") 登录
		| 
		a(href="/register") 注册